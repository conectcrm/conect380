# ADM-301 Deploy Admin-Web (2026-03-02 13:21:51)

## Contexto

Publicacao isolada do `admin-web` usando stack dedicada (`docker-compose.admin-web.yml`) com backend local em `http://localhost:3001`.

## Execucao

1. Comando de deploy:
   - `powershell -ExecutionPolicy Bypass -File scripts/deploy-admin-web.ps1`
2. Validacao HTTP:
   - `curl -I http://localhost:3010` -> `HTTP/1.1 200 OK`
3. Validacao de integracao auth + BFF:
   - `POST /auth/login` (admin) -> `201`
   - `GET /admin/bff/overview` -> `200` com `success=true`
4. Validacao de controle sensivel:
   - smoke ADM-303 em modo real -> PASS
   - evidencias:
     - `docs/features/evidencias/ADM303_SMOKE_20260302-131702.json`
     - `docs/features/evidencias/ADM303_SMOKE_20260302-131702.md`

## Resultado

1. Container `conect360-admin-web` publicado e estavel:
   - `Up ... (healthy)`
   - `0.0.0.0:3010->80/tcp`
2. Deploy isolado operacional para uso local/homolog.

## Ajustes tecnicos aplicados nesta etapa

1. `docker-compose.admin-web.yml`
   - removido campo `version` obsoleto;
   - healthcheck atualizado para `http://127.0.0.1/` (evita falha por resolucao `localhost` para IPv6 no container).
2. `scripts/deploy-admin-web.ps1`
   - porta padrao exibida corretamente (`3010`);
   - espera de health com fallback por HTTP para reduzir falso negativo no startup.
